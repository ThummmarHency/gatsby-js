import React from "react";
import Layout from "../components/layout";

const Destination = () => {
  return (
    <Layout>
      <div>Destination</div>
    </Layout>
  );
};

export default Destination;
